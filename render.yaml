services:
  - type: static
    name: email-brain-visualization
    runtime: static
    buildCommand: echo "Static site - no build needed"
    staticPublishPath: .
    branch: main
    autoDeploy: true
